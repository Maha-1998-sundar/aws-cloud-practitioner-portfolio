# Linux Notes

This file contains commonly used Linux commands and concepts
documented during hands-on practice.

---

## 📁 File & Directory Management

- `ls` – List files and directories
- `pwd` – Show current directory
- `cd` – Change directory
- `mkdir` – Create directory
- `rm -r` – Remove directory recursively

---

## 📄 File Operations

- `touch file.txt` – Create empty file
- `nano file.txt` – Edit file
- `cat file.txt` – View file content
- `cp` – Copy files
- `mv` – Move or rename files

---

## 🔐 Permissions

- `chmod` – Change file permissions
- `chown` – Change ownership

---

## 📦 Package Management (Ubuntu)

- `sudo apt update`
- `sudo apt install <package>`
- `sudo apt remove <package>`

---

## 🧠 Learning Outcome

- Improved Linux command-line confidence
- Understood directory structures
- Practiced file permissions and system commands
